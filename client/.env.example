# API base is always relative — /api and /auth are proxied by the Vite dev
# proxy (vite.config.js -> localhost:5000) locally and by Vercel's rewrites
# (client/vercel.json -> Render) in production. No env var is needed.